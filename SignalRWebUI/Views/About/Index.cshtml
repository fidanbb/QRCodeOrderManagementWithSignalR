@model List<ResultAboutDto>
@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="content">
	<div class="container-fluid">
		<h4 class="page-title">Home Page About Operations</h4>
		<div class="row">
			<div class="col-md-12">
				<div class="card">
					<div class="card-header">
						<div class="card-title">About List</div>
					</div>
					<div class="card-body">
						<div class="card-sub">
							You can perform about-related operations below: Don't forget to create only one about
						</div>
						<table class="table mt-3">
							<thead>
								<tr>
									<th scope="col">#</th>
									<th scope="col">Title</th>
									<th scope="col">Description</th>
									<th scope="col">Delete</th>
									<th scope="col">Update</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var item in Model)
								{
									<tr>
										<td>@item.AboutId</td>
										<td>@item.Title</td>
										<td>@item.Description</td>
										<td>
											<a asp-controller="About" asp-action="DeleteAbout" asp-route-id="@item.AboutId" class="btn btn-outline-danger btn-rounded"
											   onclick="return confirm('Are you sure you want to delete this about?')">Delete</a>
										</td>
										<td><a asp-controller="About" asp-action="UpdateAbout" asp-route-id="@item.AboutId" class="btn btn-outline-success btn-rounded">Update</a></td>
									</tr>
								}


							</tbody>
						</table>
						<a asp-controller="About" asp-action="CreateAbout" class="btn btn-outline-primary">Add New About</a>

					</div>
				</div>



			</div>
		</div>
	</div>
</div>